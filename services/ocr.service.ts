import { FaturaExtraida } from "../types/FaturaExtraida";

export async function extrairDadosFatura(
  pdfUrl: string
): Promise<FaturaExtraida> {

  /**
   * Aqui entraremos com a API da IA
   */

  throw new Error("OCR ainda não implementado.");
}